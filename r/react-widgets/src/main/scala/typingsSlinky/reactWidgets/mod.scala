package typingsSlinky.reactWidgets

import org.scalablytyped.runtime.Shortcut
import typingsSlinky.react.mod.Component
import typingsSlinky.reactWidgets.calendarMod.CalendarClass
import typingsSlinky.reactWidgets.calendarMod.CalendarProps
import typingsSlinky.reactWidgets.comboboxMod.ComboboxClass
import typingsSlinky.reactWidgets.comboboxMod.ComboboxProps
import typingsSlinky.reactWidgets.dateTimePickerMod.DateTimePickerClass
import typingsSlinky.reactWidgets.dateTimePickerMod.DateTimePickerProps
import typingsSlinky.reactWidgets.dropdownListMod.DropdownListClass
import typingsSlinky.reactWidgets.dropdownListMod.DropdownListProps
import typingsSlinky.reactWidgets.multiselectMod.MultiselectClass
import typingsSlinky.reactWidgets.multiselectMod.MultiselectProps
import typingsSlinky.reactWidgets.numberPickerMod.NumberPickerClass
import typingsSlinky.reactWidgets.numberPickerMod.NumberPickerProps
import typingsSlinky.reactWidgets.selectListMod.SelectListClass
import typingsSlinky.reactWidgets.selectListMod.SelectListProps
import org.scalablytyped.runtime.StObject
import scala.scalajs.js
import scala.scalajs.js.`|`
import scala.scalajs.js.annotation.{JSGlobalScope, JSGlobal, JSImport, JSName, JSBracketAccess}

object mod {
  
  object Calendar extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "Calendar")
    @js.native
    class ^ protected ()
      extends Component[CalendarProps, js.Object, js.Any] {
      def this(props: CalendarProps) = this()
      def this(props: CalendarProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "Calendar")
    @js.native
    val ^ : CalendarClass = js.native
    
    type _To = CalendarClass
    
    /* This means you don't have to write `^`, but can instead just say `Calendar.foo` */
    override def _to: CalendarClass = ^
  }
  
  object Combobox extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "Combobox")
    @js.native
    class ^ protected ()
      extends Component[ComboboxProps, js.Object, js.Any] {
      def this(props: ComboboxProps) = this()
      def this(props: ComboboxProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "Combobox")
    @js.native
    val ^ : ComboboxClass = js.native
    
    type _To = ComboboxClass
    
    /* This means you don't have to write `^`, but can instead just say `Combobox.foo` */
    override def _to: ComboboxClass = ^
  }
  
  object DateTimePicker extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "DateTimePicker")
    @js.native
    class ^ protected ()
      extends Component[DateTimePickerProps, js.Object, js.Any] {
      def this(props: DateTimePickerProps) = this()
      def this(props: DateTimePickerProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "DateTimePicker")
    @js.native
    val ^ : DateTimePickerClass = js.native
    
    type _To = DateTimePickerClass
    
    /* This means you don't have to write `^`, but can instead just say `DateTimePicker.foo` */
    override def _to: DateTimePickerClass = ^
  }
  
  object DropdownList extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "DropdownList")
    @js.native
    class ^ protected ()
      extends Component[DropdownListProps, js.Object, js.Any] {
      def this(props: DropdownListProps) = this()
      def this(props: DropdownListProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "DropdownList")
    @js.native
    val ^ : DropdownListClass = js.native
    
    type _To = DropdownListClass
    
    /* This means you don't have to write `^`, but can instead just say `DropdownList.foo` */
    override def _to: DropdownListClass = ^
  }
  
  object Multiselect extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "Multiselect")
    @js.native
    class ^ protected ()
      extends Component[MultiselectProps, js.Object, js.Any] {
      def this(props: MultiselectProps) = this()
      def this(props: MultiselectProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "Multiselect")
    @js.native
    val ^ : MultiselectClass = js.native
    
    type _To = MultiselectClass
    
    /* This means you don't have to write `^`, but can instead just say `Multiselect.foo` */
    override def _to: MultiselectClass = ^
  }
  
  object NumberPicker extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "NumberPicker")
    @js.native
    class ^ protected ()
      extends Component[NumberPickerProps, js.Object, js.Any] {
      def this(props: NumberPickerProps) = this()
      def this(props: NumberPickerProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "NumberPicker")
    @js.native
    val ^ : NumberPickerClass = js.native
    
    type _To = NumberPickerClass
    
    /* This means you don't have to write `^`, but can instead just say `NumberPicker.foo` */
    override def _to: NumberPickerClass = ^
  }
  
  object SelectList extends Shortcut {
    
    /* This class was inferred from a value with a constructor. In rare cases (like HTMLElement in the DOM) it might not work as you expect. */
    @JSImport("react-widgets", "SelectList")
    @js.native
    class ^ protected ()
      extends Component[SelectListProps, js.Object, js.Any] {
      def this(props: SelectListProps) = this()
      def this(props: SelectListProps, context: js.Any) = this()
    }
    
    @JSImport("react-widgets", "SelectList")
    @js.native
    val ^ : SelectListClass = js.native
    
    type _To = SelectListClass
    
    /* This means you don't have to write `^`, but can instead just say `SelectList.foo` */
    override def _to: SelectListClass = ^
  }
}
